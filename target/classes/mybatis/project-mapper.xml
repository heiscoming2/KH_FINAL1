<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Project">
	
	<insert id ="insert" parameterType="projectInsertDto">
		INSERT ALL
		INTO BOARD
		VALUES(BOARDSEQ.NEXTVAL, #{bd_title}, #{bd_content}, SYSDATE, NULL,
		#{bd_writerip}, 0, 0, 0} )
		INTO PROJECT
		VALUES(
		BOARDSEQ.CURRVAL,
		#{pro_title}, #{pro_start}, #{pro_end}, #{pro_link}, #{pro_develop},
		#{pro_goal}, #{pro_function}, #{pro_erd} )
	</insert>
	
	
	
	
	
	

	<select id="selectList" resultType="ProjectListDto">
		SELECT * FROM PROJECT
	</select>
	
	<delete id="deletelist">
		DELETE FROM PROJECT
	</delete>
	
	
</mapper>